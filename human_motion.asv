% ackerman.m programmed by Tomo Furukawa
function [dxk, dyk, dthk] = human_motion(xk, yk, thk, fi, si)

L = 0.5;
R_w = 0.1;
ratio = 8;
vi = wmi;
%alpha = 0.5;

dxk = fi.*cos(thk);
dyk = fi*sin(thk);
dthk = thk;
%dth = alpha / L .* gi;

